services:
  devops-nginx:
    image: devops-nginx:1.27.4-bookworm-perl
    
    ports:
      - 80:80
      - 443:443

    container_name: devops-nginx

    networks:
      - devops


networks:
  devops:
    name: devops
    external: true
